<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">
          <img alt="Brand" src="/assets/img/trophy.ico">
      </a>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
      </button>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            Tournaments <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            {{#if model.newTournaments}}
              <li class="dropdown-header">New Tournaments</li>
              {{#each model.newTournaments as |tournament|}}
                <li>
                  {{#link-to 'tournaments.play.matches' tournament}}
                    {{tournament.title}}
                  {{/link-to}}
                </li>
              {{/each}}
              <li role="separator" class="divider"></li>
            {{/if}}
            {{#if model.activeTournaments}}
              <li class="dropdown-header">Active Tournaments</li>
              {{#each model.activeTournaments as |tournament|}}
                <li>
                  {{#link-to 'tournaments.play.matches' tournament}}
                    {{tournament.title}}
                  {{/link-to}}
                </li>
              {{/each}}
              <li role="separator" class="divider"></li>
            {{/if}}
            <li>
              {{#link-to 'tournaments.archive'}}Archived tournaments{{/link-to}}
            </li>
          </ul>
        </li>
        {{#link-to "players" tagName="li" href="false"}}
          {{link-to 'Players' 'players' role='presentation'}}
        {{/link-to}}
      </ul>
    </div>
  </div>
</nav>
{{outlet}}
