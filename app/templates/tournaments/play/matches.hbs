<div class="container">
    <div class="row">
        {{#each match in this.completedMatches}}
            <div class="col-lg-3 col-md-4 col-sm-6 col-xs-12">
                <div class="panel panel-primary">
                    <div class="panel-heading"></div>
                    <div class="panel-body">
                        <table class="table">
                            <tr {{bind-attr class="match.homeWinner:info"}}>
                                <td class="col-xs-9">{{match.homePlayer.name}}</td>
                                <td class="col-xs-3">{{match.homeScore}}</td>
                            </tr>
                            <tr {{bind-attr class="match.awayWinner:info"}}>
                                <td class="col-xs-9">{{match.awayPlayer.name}}</td>
                                <td class="col-xs-3">{{match.awayScore}}</td>
                            </tr>
                        </table>
                        <a href="#" {{action 'undoMatch' match}}>
                            <span class="glyphicon glyphicon-remove"></span>
                        </a>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
    <div class="row">
        {{#each match in this.scheduledMatches}}
            <div class="col-lg-3 col-md-4 col-sm-6 col-xs-12">
                <div class="panel panel-info">
                    <div class="panel-heading"></div>
                    <div class="panel-body">
                        <table class="table">
                            <tr>
                                <td class="col-xs-8">{{match.homePlayer.name}}</td>
                                <td class="col-xs-4">
                                    {{input valueBinding="match.homeScore" type="number" class="form-control"}}
                                </td>
                            </tr>
                            <tr>
                                <td class="col-xs-8">{{match.awayPlayer.name}}</td>
                                <td class="col-xs-4">
                                    {{input valueBinding="match.awayScore" type="number" class="form-control"}}
                                </td>
                            </tr>
                        </table>
                        <a href="#" {{action 'completeMatch' match}}>
                            <span class="glyphicon glyphicon-ok"></span>
                        </a>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>
